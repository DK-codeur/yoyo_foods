import 'package:flutter/material.dart';

class RestoOfflineScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Aucun restaurant offline'),
    );
  }
}